---
"@hey-api/openapi-python": patch
---

**plugin(@hey-api/python-sdk)**: add `pagination`, returning a page that yields the items of every page from a list operation, whether the operation continues by cursor or by page number
